# Antigravity / Gemini workspace context

Read and follow `AGENTS.md` before changing the project. If the task touches
`mobile/`, also read `mobile/AGENTS.md`.

Treat `mobile/` as the canonical product UI. The root `src/` client is retained
only as a legacy/reference implementation.
